# 결정 로그 — Agent Studio

> 이 파일은 "무엇을 하기로 정했나" 의 정본이다. 규칙(`.claude/rules/`)은 세션 시작 시 자동으로 읽히지만 결정은 읽히지 않으므로, **일을 시작하기 전에 이 파일을 이번 작업의 주제어로 한 번 검색한다** (`grep -n -i "<주제어>" decisions.md`). 앞선 결정을 고쳐 쓰지 않고, 뒤집을 때는 새 결정을 아래에 덧붙인다. 규약은 [`.claude/rules/past-decisions.md`](.claude/rules/past-decisions.md).

결정 1~7 은 이 저장소가 생기기 전, 2026-09-23 ~ 2026-09-25 의 기획 대화에서 확정된 것을 옮겨 적은 것이다. 근거 문서는 [`docs/product/`](docs/product/) 에 있다.

---

### 결정 1 (2026-09-23) — 제품의 중심은 Studio 의 화면과 업무 흐름이다. Paperclip 을 다시 꾸미는 제품이 아니다.

- **정한 것**: 사용자는 Slack 처럼 대화하고 Dify 처럼 Agent 와 Skill 을 Flow 에 배치한다. Paperclip 같은 관리 계층은 필요하면 뒤에서 쓰는 부품이지, 사용자에게 보이는 제품이 아니다.
- **왜**: 흐름도만 있는 제품(Paperclip 계열)과 대화만 있는 제품(Buzz · Hermes 계열)이 따로 생긴 이유가, 사용자는 "지금 어디까지 했어" 같은 수시 대화와 여러 업무의 진행 상태를 **둘 다** 원하기 때문이라고 판단했다. 우리 제품은 그 둘을 한 업무 안에서 잇는다.
- **되돌리기**: 어렵다. 화면 구조 전체가 이 결정 위에 선다.

### 결정 2 (2026-09-24) — 디자인 기준: Chat 은 Slack, Flow 는 Dify. 버튼·기능명은 영어, 사용자 내용은 한국어. 색은 회색 계열 + 강조색 하나.

- **정한 것**: 화면 이름은 Workspace · Chat · Flow · Agents · Inbox, 주요 버튼은 New Flow · Open · Review · Approve · Request Changes · Add Agent · Add Skill · Save. 업무 제목·지시·대화·결과물은 한국어. `Run` 은 실제 AI 실행이 붙을 때만 쓰고, 그 전에는 쓰지 않는다.
- **왜**: 이전 시안이 "글이 그림을 압도" 하고 색이 많아 프로토타입처럼 보였다. 기능명을 영어로 통일하면 설명문이 줄고, 색을 줄이면 상태가 오히려 잘 읽힌다.
- **되돌리기**: 쉽다. 표기와 스타일의 문제다.

### 결정 3 (2026-09-24) — AI 연결은 사용자가 직접 로그인한 공식 클라이언트의 구독을 쓴다. 토큰을 복사하거나 API 키를 주입하지 않는다.

- **정한 것**: Claude 는 공식 Claude Code, ChatGPT 는 Codex app-server 경로. 화면에서는 **Runtime(실행기) · Model(모델) · Access(이용권)** 세 칸으로 구분해 보여 준다. Kimi · Grok · Qwen · DeepSeek 는 같은 `AgentRuntime` 인터페이스 뒤에 붙는 후보이며, 각 공급사의 구독 약관(무인 자동화·재판매 제한)을 제품화 전에 확인한다.
- **왜**: 여러 사람이 쓰는 제품에서 개인 구독 인증을 대신 갖는 방식은 약관과 보안 양쪽에서 위험하다. 공식 클라이언트에 사용자가 직접 로그인하는 경로만이 안전하게 성립한다.
- **되돌리기**: 어렵다. 인증 경계는 구조다.

### 결정 4 (2026-09-25) — 코드의 기준은 GitHub 의 PR 이다. Studio 의 "내부 검토 완료" 는 GitHub 병합을 대신하지 않는다.

- **정한 것**: 브랜치 · 커밋 · 검사 · 리뷰 · 병합은 GitHub 의 상태를 그대로 비춘다. Studio 는 업무 · 대화 · 내부 검토 결정 · 미리보기만 소유한다. 상세 규칙은 [`docs/plan/00-domain-contract.md`](docs/plan/00-domain-contract.md).
- **왜**: 이 제품이 태어난 문제가 "여러 클라우드 세션이 브랜치와 PR 을 스스로 만들어 무엇이 최신인지 잃었다" 이다. Studio 가 코드 상태를 또 하나 만들면 기준이 둘이 되어 같은 문제가 재현된다.
- **되돌리기**: 어렵다. 데이터 모델의 뼈대다.

### 결정 5 (2026-09-25) — 첫 제품의 성공 기준은 두 가지다: ① 저장소 5개 이상의 PR 을 혼동 없이 업무에 보이기, ② PR 1건을 Mac Pro 에서 실행해 휴대전화에서 미리보기. AI Run 은 그 다음이다.

- **정한 것**: 개발 순서는 0 계약 정의 → 1 PR 모으기 → 2 원격 미리보기 → 3 Studio 에서 AI 실행. 자동 브랜치 생성 · 자동 병합 · 여러 개발 호스트 운영은 첫 제품의 성공 기준에 넣지 않는다.
- **왜**: 이전 경험(Paperclip)에서 화면을 먼저 완성한 뒤 인증 연결에서 막혔다. 이번에는 사용자가 지금 겪는 고통(PR 혼동, 밖에서 검증 불가)을 먼저 풀고, 실행 연결은 검증된 뒤에 붙인다.
- **되돌리기**: 쉽다. 순서의 문제다. 다만 순서를 바꾸면 그 이유를 새 결정으로 적는다.

### 결정 6 (2026-09-25) — 사용자에게 브랜치 · 세션 인계 절차를 보이지 않는다. 판단할 수 없는 PR 만 Inbox 에서 사람에게 묻는다.

- **정한 것**: 사용자의 동선은 "프로젝트를 한 번 연결 → Workspace 에서 업무 선택 → Chat 에서 요청과 PR 카드 확인 → 필요할 때만 Open Preview → Review". 클라우드와 Mac Pro 사이의 브랜치 · 커밋 · 서버 위치는 뒤에서 관리한다.
- **왜**: 시스템 내부 절차를 사용자 절차처럼 설명하니 "업무 프로세스가 너무 복잡하다" 는 지적이 나왔다. 복잡함은 제품이 삼키고, 예외만 사람에게 올린다.
- **되돌리기**: 쉽다.

### 결정 7 (2026-09-25) — Agent 의 "소개" 는 사람이 읽는 설명이고, "지시문" 은 AI 에 전달할 프롬프트다. 선택지에는 실제로 연결 · 확인된 모델만 보인다.

- **정한 것**: Agent 편집 화면은 이름 · What it does(소개) · Instructions(지시문, 프롬프트) · AI model(Runtime · Model · Access) · Skills 로 구성한다. 예시 모델 이름을 "연결 완료" 처럼 보이거나, 실행되지 않는 Run 버튼을 두지 않는다.
- **왜**: 이전 시안의 Role 칸은 카드에 표시되는 설명일 뿐 AI 에 전달되지 않았다. "겉으로만 만들어 놓은" 칸을 없애려면 각 칸이 어디로 가는지가 정의돼야 한다.
- **되돌리기**: 쉽다. 다만 이 저장소에 반입된 시안 판본에는 아직 이 구조가 반영되지 않았다([`docs/product/README.md`](docs/product/README.md) 참고).

### 결정 8 (2026-09-25) — 기술 스택은 TypeScript 한 벌이다: Next.js 웹 + Node 서버 + PostgreSQL. 창고의 웹앱 환경 표준을 그대로 쓴다.

- **정한 것**: 화면과 서버를 한 언어(TypeScript)로 만든다. 웹은 Next.js, 저장은 PostgreSQL(로컬은 창고 `templates/webapp-environment/` 의 Docker PostgreSQL), 마이그레이션은 `db/migrations/` 에 시각 형식 파일명으로 쌓는다. 구조는 클린 아키텍처를 따른다 — 도메인 규칙은 Next.js · GitHub · 데이터베이스를 모르고, 바깥 세계와는 포트(인터페이스)로만 닿는다.
- **왜**: GitHub 연동 도구, Claude Code, Codex app-server 가 모두 TypeScript/Node 로 제공되어 연동 코드가 가장 짧다. 창고의 환경 표준과 자동 검사가 이미 이 스택을 전제로 있다. 2026-09-25 착수 보고의 결정 카드에서 옵션 A 로 승인됐다.
- **되돌리기**: 어렵다. 다만 도메인 규칙을 프레임워크와 분리해 두므로, 화면 프레임워크만 바꾸는 비용은 그만큼 줄어든다.

### 결정 9 (2026-09-25) — 잘못 연결한 PR 을 되돌리는 기능(Unlink)은 PostgreSQL 저장과 같은 단위에 넣는다.

- **정한 것**: 1단계 두 번째 단위에서 저장을 PostgreSQL 로 옮기면서, 사람이 연결을 풀어 PR 을 Inbox 로 돌려보내는 기능을 함께 만든다. 사람이 푼 연결은 표식이 있어도 다음 동기화에서 자동으로 다시 붙지 않는다.
- **왜**: 저장이 영구해지는 순간부터 잘못된 연결도 영구해진다. 계약은 "잘못 붙인 PR 은 놓친 PR 보다 해롭다" 고 적고 있다. 첫 단위 보고의 결정 요청 1 에서 추천안 A 로 승인됐다.
- **되돌리기**: 쉽다.

### 결정 10 (2026-09-25) — 복제본(fork)에서 온 PR 은 업무 표식이 있어도 자동 연결하지 않고 늘 Inbox 로 보낸다.

- **정한 것**: 자동 연결은 PR 의 브랜치가 그 저장소 자신에 있을 때만 한다. 다른 저장소(복제본)의 브랜치에서 온 PR 은 표식과 무관하게 Inbox 에 두고, 사람이 확인해 연결한다. 복제본 여부를 알 수 없으면 복제본으로 본다.
- **왜**: 표식은 PR 본문에 적는 글자일 뿐이라, 공개 저장소에서는 팀 밖의 누구나 복제본 PR 로 우리 업무에 자기 PR 을 끼워 넣을 수 있다. 첫 단위 보고의 결정 요청 2(보안)에서 추천안 A 로 승인됐다.
- **되돌리기**: 쉽다.

### 결정 11 (2026-09-25) — 진짜 GitHub 는 GitHub App 으로 읽는다. 권한은 읽기뿐이고, 웹훅 없이 Sync 로 가져온다.

- **정한 것**: 사용자가 GitHub 에서 Agent Studio 전용 GitHub App 을 만들어 읽을 저장소에만 설치한다. 권한은 Pull requests · Checks · Commit statuses · Contents · Metadata 의 **읽기**뿐이다. Studio 는 App 의 비밀 키로 짧게 사는 설치 토큰을 받아 GET 요청만 보낸다. 웹훅은 공개 주소가 생길 때까지 끄고, 화면의 Sync 버튼과 주기 동기화로 가져온다. 개인 토큰(`GITHUB_TOKEN`) 경로는 개발용으로만 남긴다.
- **함께 정한 기본값**: Inbox 에는 **열린** PR 만 두고, 연결되지 않은 채 닫히거나 병합된 PR 은 개수만 보인다(첫 단위 결정 큐의 기본값을 확정). 계약 §4 에 반영한다.
- **왜**: App 은 저장소별로 좁은 읽기 권한만 줄 수 있고, 토큰이 한 시간 안에 만료되며, 개인 계정 토큰이 퍼질 위험이 없다. 2026-09-25 사용자가 "GitHub App 으로 진행" 으로 승인했다.
- **되돌리기**: 쉽다. 연결 방식은 `GitHubReader` 포트 뒤의 어댑터 하나다.

---

## 열린 결정 — 아직 정하지 않은 것

| 번호 | 주제 | 왜 사람의 결정인가 | 현재 상태 |
|---|---|---|---|
| 열림 A | 기술 스택과 저장소 구성 | 되돌리기 어려운 구조 선택 (에스컬레이션 규칙 §2 의 3번) | **닫힘** — 결정 8 로 확정 (배포 위치는 아직 정하지 않았다) |
| 열림 B | 상속받은 창고 전용 워크플로의 처리 (`readme-skills.yml` · `project-sync.yml` 등) | CI 구성 변경은 세션이 임의로 하지 않는다 | 착수 보고의 "함께 발견한 것" 에 기록. 지시 전까지 손대지 않음 |
